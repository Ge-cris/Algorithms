// the program checks if a given number is primea
#include <iostream>
using namespace std;

#define number 17

bool isPrime (int n){
	for (int x = 2; x*x <= n; x++){
		if (n % x == 0)
			return false;
	}
	return true;
}
int main() {
	isPrime(number) ? cout<<"True" : cout<<"False";;
	return 0;
}
